apiVersion: v1
kind: Secret
metadata:
  name: indexer-secrets
  namespace: mymindinai
type: Opaque
stringData:
  AWS_REGION: "REPLACE_WITH_REGION"
  SQS_URL: "REPLACE_WITH_SQS_URL"
  DATABASE_URL: "REPLACE_WITH_DATABASE_URL"
# NOTE: Encrypt this file with SOPS before deploying to Flux-managed clusters.

